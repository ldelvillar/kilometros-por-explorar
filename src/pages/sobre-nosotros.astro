---
import { Image } from 'astro:assets';
import Layout from '@/layouts/Layout.astro';
import Timeline from '@/components/Timeline.astro';
import Cta from '@/components/Cta.astro';
import { getWebPageSchema, getCombinedSchema } from '@/config/schemas';

const title = 'Sobre Nosotros - Kilómetros por explorar';
const description =
  'Conoce al equipo detrás de Kilómetros por explorar, una pareja apasionada por crear experiencias de viaje inolvidables. Descubre nuestra historia y filosofía.';
const pathname = '/sobre-nosotros';

// Schema configuration
const pageSchema = getWebPageSchema(title, description, pathname);
const schema = getCombinedSchema(pageSchema);
---

<Layout title={title} description={description} schema={schema}>
  <main class="animate-fade-in-up">
    <section class="px-4 pt-24 pb-20 mx-auto max-w-6xl">
      <h1 class="mb-4 text-4xl font-extrabold text-gray-800 tracking-tight">
        Sobre Nosotros
      </h1>
      <Image
        src="/images/team/couple.webp"
        alt="Foto de los fundadores de Kilómetros por explorar"
        class="md:hidden my-6 size-24 justify-self-center rounded-full"
        width={100}
        height={100}
        loading="eager"
      />
      <p class="text-lg text-gray-600">
        Conoce al equipo detrás de Kilómetros por explorar, una pareja
        apasionada por crear experiencias de viaje inolvidables.
      </p>
      <div class="mt-12 flex flex-col md:flex-row items-center gap-4">
        <Image
          src="/images/team/couple.webp"
          alt="Foto de los fundadores de Kilómetros por explorar"
          class="hidden md:block size-24 rounded-full"
          width={100}
          height={100}
          loading="eager"
        />
        <div class="max-w-4xl">
          <h2 class="text-xl font-semibold text-gray-800">Nuestra Historia</h2>
          <p class="text-gray-600">
            Somos una pareja de viajeros que decidió convertir su pasión por
            explorar el mundo en un estilo de vida. Desde nuestras primeras
            aventuras juntos, supimos que queríamos compartir nuestras
            experiencias y ayudar a otros a descubrir la belleza de viajar. Así
            es cómo nació Kilómetros por Explorar.
          </p>
        </div>
      </div>

      <h2 class="mt-14 text-xl font-semibold text-gray-800">
        Nuestra Filosofía
      </h2>
      <p class="max-w-5xl text-gray-600">
        Creemos que viajar es una de las experiencias más enriquecedoras que
        existen. Nos esforzamos por ofrecer itinerarios personalizados que se
        adapten a los intereses y necesidades de cada viajero, asegurando que
        cada viaje sea único e inolvidable. Además, promovemos el turismo
        responsable y sostenible, respetando las culturas y el medio ambiente de
        los destinos que visitamos.
      </p>

      <h2 class="mt-14 text-xl font-semibold text-gray-800">Nuestro Viaje</h2>
      <Timeline />

      <h2 class="mt-14 text-xl font-semibold text-gray-800">
        Conoce al equipo
      </h2>
      <div class="mt-4 flex justify-center gap-12">
        <div class="flex flex-col items-center">
          <Image
            src="/images/team/lucas.webp"
            alt="Foto de Lucas"
            class="size-32 md:size-48 rounded-full"
            width={100}
            height={100}
            loading="lazy"
          />
          <h3 class="mt-4 text-lg font-medium text-gray-800">
            Lucas del Villar
          </h3>
          <p class="text-gray-600 text-sm">Co-Fundador & CEO</p>
        </div>

        <div class="flex flex-col items-center">
          <Image
            src="/images/team/lucia.webp"
            alt="Foto de Lucía"
            class="size-32 md:size-48 rounded-full"
            width={100}
            height={100}
            loading="lazy"
          />
          <h3 class="mt-4 text-lg font-medium text-gray-800">Lucía Ruiz</h3>
          <p class="text-gray-600 text-sm">Co-Fundadora & CEO</p>
        </div>
      </div>
    </section>
    <Cta />
  </main>

  <style>
    /* Custom animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in-up {
      animation: fadeInUp 0.6s ease-out forwards;
    }
  </style>
</Layout>
